import React from "react";
const initalalState = {};
const Reducer = (state = initalalState, action) => {
  switch (action) {
    case value:
      break;

    default:
      break;
  }
  return state;
};
export default Reducer;
